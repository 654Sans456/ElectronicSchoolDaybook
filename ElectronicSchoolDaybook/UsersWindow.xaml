<Window x:Class="ElectronicSchoolDaybook.UsersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectronicSchoolDaybook"
        mc:Ignorable="d"
        Title="UsersWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Удалить" Click="DeleteMenuItem_Click"/>
        </Menu>
        
        <DataGrid Name="UsersDataGrid" AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  SelectionMode="Single"
                  SelectionChanged="UsersDataGrid_SelectionChanged"
                  RowEditEnding="UsersDataGrid_RowEditEnding"
                  Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                <DataGridTextColumn Header="Password" Binding="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
                <DataGridTextColumn Header="RoleID" Binding="{Binding RoleID, UpdateSourceTrigger=PropertyChanged}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>

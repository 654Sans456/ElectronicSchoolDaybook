<Window x:Class="ElectronicSchoolDaybook.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0">
            <Label Content="Логин:"/>
            <TextBox Name="txtUsername" Width="250"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <Label Content="Пароль:"/>
            <PasswordBox Name="txtPassword" Width="250"/>
        </StackPanel>
        
        <Button Content="Войти" Grid.Row="2" Margin="0,10,0,0" Width="100" HorizontalAlignment="Center" Click="btnLogin_Click"/>
    </Grid>
</Window>
